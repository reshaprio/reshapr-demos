# Demos

Each demo directory contains a `demo.sh` file. Run a demo directly:

```sh
./demos/03-github-graphql-context/demo.sh
```

Or run it through the root npm alias:

```sh
npm run demo:03
```

The direct script is the canonical entrypoint. The npm alias is provided for convenience when working from the repository root.
