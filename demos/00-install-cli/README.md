# Demo 00: Install CLI

Run from this directory:

```sh
./demo.sh
```

Run from the repository root:

```sh
./demos/00-install-cli/demo.sh
```

Or use the npm alias from the repository root:

```sh
npm run demo:00
```
