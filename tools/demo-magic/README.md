# demo-magic

This directory contains the open source `demo-magic.sh` script used by the shell demos.

Upstream project: https://github.com/paxtonhare/demo-magic

To update it, replace `demo-magic.sh` with the upstream version and verify the demos still run.
